def isCorrectBracket(s):
    type_a = [] # []
    type_b = [] # ()
    type_c = [] # {}
    for bracket in s:
        if bracket in ['[',']']:
            type_a.append(bracket)
        elif bracket in ['(',')']:
            type_b.append(bracket)
        else:
            type_c.append(bracket)
    for bracket in s:
        if bracket == ']':
            
            

def solution(s):

    for bracket in s:
        if 
    return answer